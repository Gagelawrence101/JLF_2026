# Raw shot data -- lives on the shared drive, not in git.
# (no trailing slash -- SLUG/data is a symlink locally, not a real dir, and
# a trailing-slash pattern only matches real directories)
SLUG/data
SLUG.tar
*.h5
*.wfm
shot*.csv
tek*.csv
drive-download-*/
drive-download-*.zip

# Jupyter / Python housekeeping
.ipynb_checkpoints/
__pycache__/
*.pyc
.venv/

# Local machine-specific config
.claude/
